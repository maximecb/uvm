#include <assert.h>
#include <string.h>
#include <stdlib.h>

// Linked-list node
typedef struct {
    int v;
    node_t* next;
} node_t;

int main()
{
    // FIXME: this doesn't quite work yet
    /*
    node_t* list = NULL;

    for (int i = 0; i < 10; ++i)
    {
        node_t* new_node = (node_t*)malloc(sizeof(node_t));
    }
    */

    return 0;
}
